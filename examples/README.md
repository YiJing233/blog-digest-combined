# Sample briefs

These are real anonymized excerpts from the 2026-09-22 跑 produced by
`ae7df3150e0e` (iCloud 订阅) and `909cbd821c57` (Indie 科技).

Used as fixtures in `tests/test_fetcher.py` and `tests/test_cli.py`.
